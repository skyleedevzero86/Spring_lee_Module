<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sleekydz86.passykey.adapter.outbound.persistence.UserMyBatisMapper">

    <resultMap id="userResultMap" type="User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="displayName" column="display_name"/>
        <result property="role" column="role"/>
        <result property="accountLocked" column="account_locked"/>
        <result property="accountExpired" column="account_expired"/>
        <result property="credentialsExpired" column="credentials_expired"/>
        <result property="enabled" column="enabled"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="save" statementType="CALLABLE" parameterType="map" resultType="map">
        {CALL sp_save_user(
            #{operation, mode=IN, jdbcType=CHAR},
            #{id, mode=IN, jdbcType=BIGINT},
            #{username, mode=IN, jdbcType=VARCHAR},
            #{password, mode=IN, jdbcType=VARCHAR},
            #{email, mode=IN, jdbcType=VARCHAR},
            #{displayName, mode=IN, jdbcType=VARCHAR},
            #{userHandle, mode=IN, jdbcType=VARCHAR},
            #{enabled, mode=IN, jdbcType=BOOLEAN},
            #{accountNonExpired, mode=IN, jdbcType=BOOLEAN},
            #{accountNonLocked, mode=IN, jdbcType=BOOLEAN},
            #{credentialsNonExpired, mode=IN, jdbcType=BOOLEAN},
            #{resultId, mode=OUT, jdbcType=BIGINT}
        )}
    </select>

    <select id="selectById" parameterType="Long" resultMap="userResultMap">
        SELECT id, username, password, email, display_name, 
               role, account_locked, account_expired, credentials_expired,
               enabled, created_at, updated_at
        FROM users WHERE id = #{id}
    </select>

    <select id="selectByUsername" parameterType="String" resultMap="userResultMap">
        SELECT id, username, password, email, display_name, 
               role, account_locked, account_expired, credentials_expired,
               enabled, created_at, updated_at
        FROM users WHERE username = #{username}
    </select>

    <select id="selectByEmail" parameterType="String" resultMap="userResultMap">
        SELECT id, username, password, email, display_name, 
               role, account_locked, account_expired, credentials_expired,
               enabled, created_at, updated_at
        FROM users WHERE email = #{email}
    </select>

    <select id="selectAll" resultMap="userResultMap">
        SELECT id, username, password, email, display_name, 
               role, account_locked, account_expired, credentials_expired,
               enabled, created_at, updated_at
        FROM users
    </select>

    <select id="existsByUsername" parameterType="String" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE username = #{username}
    </select>

    <select id="existsByEmail" parameterType="String" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE email = #{email}
    </select>

</mapper>